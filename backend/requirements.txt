"""
requirements.txt — JARVIS v4.0
Core dependencies. Optional ones marked — install only if needed.
"""
# ── Core ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0
pydantic>=2.8.0
python-dotenv>=1.0.0
httpx>=0.27.0

# ── AI / LLM ──
groq>=0.4.0

# ── TTS (Free, no API key) ──
edge-tts>=6.1.0

# ── Browser Automation ──
selenium>=4.15.0
webdriver-manager>=4.0.0

# ── PC Control (Optional) ──
pyautogui>=0.9.54          # Mouse, keyboard, screenshot
pygetwindow>=0.0.9         # Window listing
pyperclip>=1.8.0           # Clipboard
plyer>=2.1.0               # Desktop notifications

# ── Windows-only PC Control ──
pycaw>=20230407            # Volume control
pywin32>=306               # WMI, brightness, lock
wmi>=1.5.1                 # Windows Management Instrumentation

# ── WhatsApp ──
pywhatkit>=5.4.0           # WhatsApp messages

# ── Smart Home ──
broadlink>=0.18.0          # IR Blaster (RM Mini 3 / Pro)
tinytuya>=1.13.1           # Tuya WiFi devices

# ── Utilities ──
python-multipart>=0.0.9    # File uploads
